#ifndef __STRINGLIBRARY_H_
#define __STRINGLIBRARY_H_
#include <iostream>

void copyString( char*, const char* );

int stringLength( char* );

void concatString( char*, char* );

bool compareStrings( char*, const char* );

#endif
